1. python -m venv venv
2. venv\Scripts\activate
3. py -m pip install flask flask-cors
4. py -m pip install supabase
5. py -m pip install pandas
6. py -m pip install scikit-learn
7. py -m pip install numpy
8. supabase_env file is the same as .env.local
9. Run Recommendation_Engine.py before submitting questionnaire.
